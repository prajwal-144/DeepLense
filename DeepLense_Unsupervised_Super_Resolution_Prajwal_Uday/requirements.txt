# Lower bounds, not the exact versions the reported numbers were produced with.
numpy>=1.20
scipy>=1.7
matplotlib>=3.4

# Needed by everything under scripts/ whose name starts with train_ or eval_.
# The lens and source code in src/ is pure numpy and runs without it.
torch>=1.12

# Only tests/test_lens_models.py and tests/test_sources.py import this. They
# check the deflection field and the Sersic profile against an independent
# implementation. Drop this line if you do not want to run those two.
lenstronomy>=1.11
